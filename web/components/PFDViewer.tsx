'use client';
import { Stage5Result } from '@/lib/api';
// Full implementation in Task 5. Stub to satisfy type-check.
interface PFDViewerProps { status: 'running' | 'done'; result: Stage5Result | null; }
export function PFDViewer(_: PFDViewerProps) { return null; }
